export interface MagicLinkData {
  text: string;
  iconUrl: string | null;
  darkIconUrl?: string | null;
}

export interface MarkdownConfig {
  html: boolean;
  linkify: boolean;
  typographer: boolean;
}

